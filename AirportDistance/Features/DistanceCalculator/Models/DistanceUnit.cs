namespace AirportDistance.Features.DistanceCalculator.Models
{
    /// <summary>
    /// Represents a unit in which distance is measured between multiple airports.
    /// </summary>
    public enum DistanceUnit
    {
        Unspecified = 0,
        Mile = 1,
        Kilometer = 2,
    }
}
